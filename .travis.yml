language: minimal

script:
  - pushd docker/rel2
  - ./dockit
#  - docker run -t -v $PWD:/workdir assimilationproject/nanoprobe:2.0.0 cp /tmp/build/bin/nanoprobe /workdir
#  - file nanoprobe
#  - ldd nanoprobe
#  - ./nanoprobe
#  - docker build --build-arg VERSION -f ci/Dockerfiles/centos7_fpm.dockerfile -t centos_fpm /tmp/nanoprobework
#  - docker run -v /tmp/output:/output centos_fpm bash -c "cp /workdir/*.rpm /output"
#  - ls -al /tmp/output/*.rpm
#  - rpm -qlp /tmp/output/*.rpm
