name: build
on: [push]
jobs:
  job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
#      - run: cd $GITHUB_WORKSPACE/docker && ./dockit
      - run: DOCKER_IMAGE=$(grep -v '#' < $GITHUB_WORKSPACE/docker/nanoprobe/docker-tag.txt | head -n1)
      - run: DOCKER_TAG=$(cat $GITHUB_WORKSPACE/CHANGELOG.md | perl -ne '/^## \[(\d+\.\d+\.\d+.*)\] -/ && print "$1\n"' | head -n1)
      - run: echo $DOCKER_IMAGE
      - run: echo $DOCKER_TAG

